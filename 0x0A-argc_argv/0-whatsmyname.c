#include <stdio.h>
/**
 * main - main fxn
 * @argc: takes in arguments for main
 * @argv: array of the @argc
 *
 * Return: main returns zero
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
