#include <stdio.h>
/**
 * main - c main function
 * num: number
 * Return: main returning zero
 */
int main(void)
{
	int num;

	for (num = 0 ; num < 10 ; num++)
		printf("%d", num);
	printf("\n");
	return (0);
}
